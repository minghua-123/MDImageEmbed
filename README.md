# MD Image Embed

Obsidian 插件，用于将 Markdown 中的本地图片转换为 Base64 嵌入格式。适用于导出笔记、发布博客和分享文档。

## 功能特点

- **复制为 Base64 格式**：将当前 Markdown 文件中的本地图片转换为 Base64 格式并复制到剪贴板
- **导出为 Base64 格式文件**：将当前 Markdown 文件中的本地图片转换为 Base64 格式并导出为新文件
- **侧边栏导出按钮**：在左侧边栏添加快捷导出按钮
- **导出设置**：支持设置默认导出路径和文件名
- **双浏览模式**：支持在 Vault 内浏览和系统浏览两种方式选择文件夹
- **防转载保护**：支持添加前缀和后缀文件内容
- **详细日志**：支持显示转换过程的详细日志
- **多格式支持**：支持标准 Markdown 图片语法和 Obsidian Wiki 链接语法

## 安装方法

1. 使用 BRAT 插件安装：
   - 打开 BRAT 插件
   - 点击 "Add a beta plugin"
   - 输入 `minghua-123/MDImageEmbed`
   - 点击 "Add plugin"
   - 启用插件

2. 手动安装：
   - 下载最新版本的 `main.js`、`manifest.json` 和 `styles.css` 文件
   - 将文件复制到 Obsidian 插件目录的 `md-image-embed` 文件夹中
   - 重启 Obsidian 并启用插件

3. 从源码构建：
   ```bash
   git clone https://github.com/minghua-123/MDImageEmbed.git
   cd MDImageEmbed
   npm install
   npm run build
   ```
   - 构建完成后，将生成的 `main.js`、`manifest.json` 和 `styles.css` 文件复制到 Obsidian 插件目录的 `md-image-embed` 文件夹中
   - 重启 Obsidian 并启用插件

## 使用方法

### 方法 1：右键菜单
1. 在文件浏览器中右键点击 Markdown 文件
2. 选择 "复制为 Base64 格式" 或 "导出为 Base64 格式"

### 方法 2：侧边栏按钮
1. 打开一个 Markdown 文件
2. 点击左侧边栏的下载图标按钮
3. 在弹出的对话框中设置导出选项
4. 点击 "导出" 按钮

## 设置选项

- **显示转换日志**：在通知中显示转换摘要信息
- **显示详细日志**：在通知中显示每个图片的状态
- **转换 Wiki 链接**：将 Obsidian Wiki 链接 (![[image.png]]) 转换为标准 Markdown Base64 格式
- **跳过 Base64 图片**：跳过已经是 Base64 格式的图片
- **前缀文件路径**：添加到文章开头的 Markdown 文件路径
- **后缀文件路径**：添加到文章结尾的 Markdown 文件路径
- **默认导出路径**：导出文件的默认保存路径
- **显示侧边栏图标**：在左侧边栏显示 MD Image Embed 导出按钮

## 更新日志

### 版本 2.6.0 (2026-05-17)
- 优化：全面重构 UI/CSS 样式系统
- 新增：CSS 变量系统，支持主题适配（亮色/暗色）
- 优化：导出对话框视觉设计，添加圆角、阴影和间距
- 优化：设置面板样式，增强视觉层次和交互反馈
- 新增：微交互动画（按钮悬停、点击缩放、对话框动画）
- 优化：响应式设计，适配不同设备尺寸
- 优化：文件夹选择器交互样式
- 优化：进度条和 Notice 消息样式
- 清理：移除所有内联样式，迁移至 CSS 文件

### 版本 2.5.1 (2026-05-17)
- 修复：支持导出文件到笔记仓库外部路径（如桌面、其他磁盘分区等）
- 优化：系统浏览功能现在允许选择任意文件夹，不再限制在 Vault 内部
- 新增：自动检测导出路径类型（Vault 内部/外部），使用对应的文件写入方式
- 优化：外部路径导出时自动创建不存在的目录

### 版本 2.5.0 (2026-05-17)
- 修复：同一图片在文档中出现多次时转换不完整的问题，改为基于索引的精确替换
- 修复：导出文件已存在时崩溃的问题，改为自动覆盖
- 修复：文件名包含多个 `.md` 时替换不精确的问题
- 优化：大图片 Base64 转换性能，使用 8KB 分块处理
- 新增：长文档处理时显示实时进度通知
- 优化：导出对话框输入校验，文件名为空时禁用导出按钮
- 优化：消除核心功能的重复代码，提取公共方法
- 修复：GitHub Actions 工作流语法错误
- 清理：删除未使用的模块导入和构建产物版本控制

### 版本 2.4.1 (2026-04-14)
- 优化：导出设置对话框的按钮布局，Vault内浏览和系统浏览按钮在同一行显示
- 优化：导出文件名输入框的显示长度
- 清理：项目内废弃的文件和过时的代码

### 版本 2.4.0 (2026-04-14)
- 优化：导出设置对话框的排版，解决标签被输入框挤压的问题
- 新增：添加了 styles.css 文件，提供更好的样式支持

### 版本 2.3.0 (2026-04-14)
- 新增：添加系统文件夹浏览功能
- 新增：支持双浏览模式（Vault内浏览和系统浏览）
- 优化：输入框大小，确保路径能够完整显示
- 变更：设置为桌面端专用插件

### 版本 2.2.0 (2026-04-14)
- 修复：侧边栏按钮的实时开启/关闭功能
- 新增：导出设置对话框，支持设置导出位置和文件名
- 优化：导出路径设置，支持默认导出路径

### 版本 2.0.0 (2026-04-14)
- 新增：侧边栏导出按钮
- 新增：导出地址设置
- 优化：整体功能重构

### 版本 1.1.2 (2026-04-14)
- 新增：导出为文件功能
- 优化：导出文件命名规则

### 版本 1.1.1 (2026-04-14)
- 汉化：将所有界面元素改为中文
- 优化：通知消息和日志消息的中文显示

### 版本 1.0.0 (2026-04-14)
- 初始版本
- 实现基本的图片转 Base64 功能
- 支持复制到剪贴板
- 支持设置面板

## 技术实现

- 使用 TypeScript 开发
- 支持 Obsidian API
- 支持 Electron 系统对话框（桌面端）
- 支持 Vault 内文件操作

## 注意事项

- 本插件仅在桌面端可用
- 转换后的 Base64 格式图片会增加文件大小
- 建议仅在需要导出或分享时使用

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License
